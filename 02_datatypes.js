/*
String, Number, Boolean
BigInt

null = no value (empty), undefined = no definition

Objects, Array, Dictionaries

Symbol - Unique
*/

// var is old and problematic
// var score = 20;

// use let instead of var, var still exist in legacy codebases
let score = 20;
let name = "John";
let isLoggedIn = false;

// objects
let info = {firstname:"John", lsatname:"Doe"}
let fruits = ["apple", "guava", "graps"]
